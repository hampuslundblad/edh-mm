services:
  nginx:
    depends_on:
      - app
    restart: always
    build: .
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/ssl/cert:/etc/nginx/ssl/cert.pem
      - /etc/ssl/key.pem:/etc/nginx/ssl/key.pem

  app:
    image: ghcr.io/hampuslundblad/edh-mm-app:main
    #build: ../app
    ports:
      - "3000:3000"
